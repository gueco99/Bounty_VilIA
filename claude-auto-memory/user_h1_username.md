---
name: user-h1-username
description: "User's hacker handles and email aliases across bug bounty platforms (HackerOne, YesWeHack)"
metadata: 
  node_type: memory
  type: user
  originSessionId: af7dde18-801c-473e-bf9a-1ea1802fac29
---

Cross-platform hunter identity:

- **HackerOne**: username **gueco**. Use `X-HackerOne-Researcher: gueco` header when a program requires it (e.g. Coupang Taiwan — see [[project_coupang_tw]]). Default hacker email alias pattern is `gueco@wearehackerone.com`, but the user may override per-program (e.g. used `gueco@imnotahacker.com` for Coupang TW instead — that was a deliberate choice, not an error, don't "correct" it back).
- **YesWeHack**: email alias **gueco-ywh-cec455c3232b7382@yeswehack.ninja**. Use this for test-account signup on YWH programs (e.g. OneDoc — see [[project_onedoc_ywh]]).

**How to apply:** Pick the alias matching the platform the program is hosted on. Ask the user if a program needs an alias not yet recorded here, and save it when given.
